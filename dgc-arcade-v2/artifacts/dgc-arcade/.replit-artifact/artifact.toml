kind = "web"
previewPath = "/"
title = "DGC Arcade"
version = "1.0.0"
id = "artifacts/dgc-arcade"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20720

[services.development]
run = "pnpm --filter @workspace/dgc-arcade run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dgc-arcade", "run", "build" ]
publicDir = "artifacts/dgc-arcade/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20720"
BASE_PATH = "/"
